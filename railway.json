{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT & python scraper_worker.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
